# Servicii RO Germania (Directory MVP)
Flask + PostgreSQL + Cloudinary images + Admin + SEO landings + sitemap + radius filter.

## Local setup
### 1) Create venv + install
```bash
python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
